emit:
  - "@typespec/openapi3"

linter:
  extends:
    - "@typespec/http/all"

options:
  "@typespec/openapi3":
    emitter-output-dir: "{project-root}/dist/openapi"
    output-file: "bilibili.openapi.yaml"
    openapi-versions:
      - 3.1.0
